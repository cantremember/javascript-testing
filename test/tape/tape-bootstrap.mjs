import test from 'tape';

// // "Error: Cannot find module 'mock-require-from-bootstrap'"
// import mockRequireExport, { mockRequireProperty } from 'mock-require-from-bootstrap';


test.skip('mock-require', (t) => {
  t.plan(2);

  // t.equal(mockRequireExport, 'EXPORT');
  // t.equal(mockRequireProperty, 'PROPERTY');

  t.end();
});
